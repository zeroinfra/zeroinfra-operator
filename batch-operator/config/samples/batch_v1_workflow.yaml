apiVersion: batch.zeroinfra.github.com/v1
kind: Workflow
metadata:
  labels:
    app.kubernetes.io/name: batch-operator
    app.kubernetes.io/managed-by: kustomize
  name: workflow-sample
spec:
  # TODO(user): Add fields here
